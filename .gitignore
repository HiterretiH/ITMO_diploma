# Thesis and internal documentation (not part of the application repo)
diploma/
docs/

# Local IDE / workspace (not shared)
.ide/

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Office / LibreOffice: lock files while .docx (etc.) are open
.~lock.*
~$*

# Logs and temp
*.log
*.tmp
*.temp
*.swp
*~

# Local env / secrets (if placed at repo root)
.env
.env.*
!.env.example

# Docker local overrides
docker-compose.override.yml

# Deployment archives (generated locally)
deploy-archive.tar.gz
truck-docs-deploy.tar.gz

# Manual-review fixtures and timing reports live under backend/build/ (ignored by backend/.gitignore).
# Old fixture output directory (ignore if still present locally):
backend/manual-review-docs/
# Legacy timing file path (ignore if recreated):
backend/docs/document-generation-timing-baseline.txt

# Python
__pycache__/
*.py[cod]

# Gradle (local cache and probes)
.gradle/

# Generated by utilities/docx_to_pdf_template (keep source *.docx templates tracked)
*.form.docx

# XML-substitution POC output (utilities/check_docx_structure/generate_docx_xml_substitution.py)
utilities/check_docx_structure/out/

